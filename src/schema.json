{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CreditCardTransaction",
  "type": "object",
  "properties": {
    "trans_num": { 
        "type": "string",
        "minLength": 10,
        "description": "ID único da transação não pode ser vazio"
    },
    "cc_num": { 
        "type": "string",
        "pattern": "^\\*+[0-9]{4}$",
        "description": "Conformidade LGPD: O cartão DEVE estar mascarado, terminando com 4 dígitos numéricos"
    },
    "amt": { 
        "type": "number",
        "exclusiveMinimum": 0,
        "maximum": 500000,
        "description": "Valor deve ser maior que 0 e menor que 500.000"
    },
    "merchant": { 
        "type": "string",
        "minLength": 3,
        "description": "Nome da loja deve ter pelo menos 3 caracteres"
    },
    "trans_date_trans_time": { 
        "type": "string",
        "pattern": "^\\d{4}-\\d{2}-\\d{2} \\d{2}:\\d{2}:\\d{2}$",
        "description": "Validação de formato temporal restrito: YYYY-MM-DD HH:MM:SS"
    }
  },
  "required": ["trans_num", "cc_num", "amt", "merchant", "trans_date_trans_time"]
}